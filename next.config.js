/** @type {import('next').NextConfig} */
const nextConfig = {
  // All images are local under /public/images/ — no remote patterns needed.
  // If you add remote sources later, add them here, e.g.:
  // images: { remotePatterns: [{ protocol: "https", hostname: "cdn.example.com" }] }
};

module.exports = nextConfig;
